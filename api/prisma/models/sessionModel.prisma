model session{
    id Int @id @default(autoincrement())
    token String
    userId Int

    session User @relation(fields: [userId], references: [id])

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
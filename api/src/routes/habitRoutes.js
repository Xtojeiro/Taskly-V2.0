/*import express from 'express';
import habitController from '../controllers/habitController.js';

const router = express.Router();
router.post('/habits', habitController.createHabit);
router.get('/habits', habitController.getHabits);
router.get('/habits/:id', habitController.getHabitById);
router.put('/habits/:id', habitController.updateHabit);
router.delete('/habits/:id', habitController.deleteHabit); 

router.get('/habits/:id/logs', habitController.getHabitLogs);
router.get('/habits/:id/user-stats', habitController.getUserStats);
router.get('/habits/:id/heatmap', habitController.getHabitHeatmap);
router.get('/habit/:id/todays-stats', habitController.getTodayStats);
router.get('/habits:id/dayDetails', habitController.getdayDetails);
router.get('/habits/:id/userSettings', habitController.getuserSettings);
*/
